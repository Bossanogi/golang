package integers

// func Add take two integers and return sum of them
func Add(x, y int) int {
	return x + y
}

// func Sub take two integers and return difference first minus second
func Sub(x, y int) int {
	return x - y
}
